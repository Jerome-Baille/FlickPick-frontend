const BASE_URL = 'https://flick-pick.jerome-baille.fr/api';

export const BACKEND_API_URL = {
    user: `${BASE_URL}/user`,
    auth: `${BASE_URL}/auth`,
    group: `${BASE_URL}/group`,
    list: `${BASE_URL}/list`,
    media: `${BASE_URL}/media`,
    vote: `${BASE_URL}/vote`,
};